package strings;

public class CountCharacterOccurance {

	public static void main(String[] args) {

		
		String str = "Java Programming Practice";
		
		int strLen = str.length();
		//int totalCount = str.replaceAll("a", "").length();
		
		int count = 0; 
		
		for(int i=0; i<strLen; i++)
		{
			//count = Integer.parseInt(str.replaceAll(str.charAt(i), ""));
				
		}
		
		
		//System.out.println(strLen-totalCount);
		
		
		
	}

}
